# Shared rules configuration
# These map directly to claude-code rules, and are concatenated into memory for opencode/codex
{
  # Example structure - rules are attrsets with source files or text
  # "rule-name" = {
  #   source = ./content/rules/rule-name.md;
  #   # OR
  #   text = "Rule content here";
  # };
}
